# Raw Evidence Layout

Expected path:

```text
raw/YYYY-MM-DD/<order_id>/<engine>/<prompt_id>.json
```

Only publish raw datapoints from an approved public evidence package. Customer
evidence must use `measurement_mode=live_engine_data`.
