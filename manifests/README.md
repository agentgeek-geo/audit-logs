# Evidence Manifests

Expected path:

```text
manifests/YYYY-MM-DD/<order_id>/customer-evidence-manifest.json
```

Customer-safe manifests must omit internal local paths, private storage keys,
secrets, billing data, and customer contact details.
